echo "📤 匯出 inter 資料..."
  # ... 原本的 if-elif-else 匯出指令不變 ...

  # ==================== 🛠️ 新增：斷網與空檔案防呆檢查 ====================
  if [ ! -s "$TMP_FILE" ]; then
    echo "❌ [警告] 暫存檔 $TMP_FILE 為空或不存在！"
    echo "   可能原因：來源資料庫網路不通、連線超時、或匯出失敗。"
    echo "   ⏭️ 跳過此資料表 ($TABLE) 的同步，繼續處理下一張表。"
    continue
  fi
  # =====================================================================

  echo "🧪 建立 inter staging 表..."
  # ... 後續的建立 Table 與 INSERT INTO 步驟 ...